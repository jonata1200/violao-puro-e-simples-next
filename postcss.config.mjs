/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {}, // Para a v3, usamos 'tailwindcss' diretamente
    autoprefixer: {},
  },
}

export default config;